# .gitleaks.toml — Generated by DevOpsGen
# Add patterns here to suppress known false positives.
[allowlist]
  description = "DevOpsGen allow-list"
  regexes = [
    # Uncomment to allow known test/example tokens:
    # "AKIAIOSFODNN7EXAMPLE",
  ]
  paths = [
    # Uncomment to skip directories:
    # "test/",
    # "docs/",
  ]
